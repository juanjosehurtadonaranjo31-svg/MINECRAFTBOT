// Funciones de claim y recompensas
const addClaim = (userId) => {
  // Placeholder para agregar claim
}

const checkClaim = (userId) => {
  // Placeholder para verificar si puede hacer claim
  return false
}

const timeClaim = (userId) => {
  // Placeholder para obtener tiempo restante
  return 0
}

const expiredClaim = (userId) => {
  // Placeholder para verificar si expiró el claim
  return false
}

module.exports = {
  addClaim,
  checkClaim,
  timeClaim,
  expiredClaim
}
