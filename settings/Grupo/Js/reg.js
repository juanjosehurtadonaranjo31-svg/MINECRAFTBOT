// Funciones de economía y registro
const addkoin = (userId) => {
  // Placeholder
}

const delkoin = (userId, amount) => {
  // Placeholder
}

const AddReg = (userId) => {
  // Placeholder
}

const checkOfReg = (userId) => {
  return true
}

const addLevel = (userId) => {
  // Placeholder
}

const addXp = (userId, amount) => {
  // Placeholder
}

const levelOfsender = (userId) => {
  return 1
}

const xpOfsender = (userId) => {
  return 0
}

const checkOfRegM = (userId) => {
  return true
}

const addkoinM = (userId, amount) => {
  // Placeholder
}

const delkoinM = (userId, amount) => {
  // Placeholder
}

const MoneyOfM = (userId) => {
  return 0
}

const Rxp = (userId) => {
  return 0
}

const addRxp = (userId, amount) => {
  // Placeholder
}

const addRep = (userId) => {
  // Placeholder
}

const delRep = (userId) => {
  // Placeholder
}

const repUser = (userId) => {
  return 0
}

const MoneyOfSender = (userId) => {
  return 0
}

module.exports = {
  MoneyOfSender,
  addkoin,
  delkoin,
  AddReg,
  checkOfReg,
  addLevel,
  addXp,
  levelOfsender,
  xpOfsender,
  checkOfRegM,
  addkoinM,
  delkoinM,
  MoneyOfM,
  Rxp,
  addRxp,
  addRep,
  delRep,
  repUser
}
